name: PullRequest

on:
  pull_request:
    branches: [master]

jobs:
  build:
    name: Preview
    runs-on: [ubuntu-latest]
    env:
      ACTIONS_RUNNER_DEBUG: true
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v2
      - name: Setup
        id: generateurl
        uses: whywhy36/crafting-sandbox-action/@v0.0.14
        with:
          name: hello
